This is the original PHP prototype (audit score 2.5/10) kept for before/after comparison. See STACK-ROADMAP.md for the rebuild rationale.

It is not part of the running SeatWise app (`apps/web` + `apps/api`). The prototype had split databases, SQL injection, and XSS — those are the findings the Node rebuild was written to close.
